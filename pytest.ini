[pytest]
# Put the project root on sys.path so tests in tests/ can import the top-level
# pawpal_system module regardless of how pytest is invoked (bare `pytest`,
# `pytest --cov`, or `python -m pytest`). Requires pytest >= 7.0.
pythonpath = .
testpaths = tests
